import RouterExpressInterface from "../../../express/domain/RouterExpressInterface";

export default interface OrderRouterExpressInterface extends RouterExpressInterface {
  // Métodos específicos del router de pedidos, si los hay
}